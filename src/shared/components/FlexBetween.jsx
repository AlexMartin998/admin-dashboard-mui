import { Box, styled } from '@mui/material';

// styled component to reuse styles
export const FlexBetween = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
});
